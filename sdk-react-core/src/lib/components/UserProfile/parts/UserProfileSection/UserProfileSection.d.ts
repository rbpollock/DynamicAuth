import React from 'react';
export type UserProfileSectionProps = {
    children: React.ReactNode;
    title?: string;
};
export declare const UserProfileSection: ({ title, children, }: UserProfileSectionProps) => JSX.Element;
