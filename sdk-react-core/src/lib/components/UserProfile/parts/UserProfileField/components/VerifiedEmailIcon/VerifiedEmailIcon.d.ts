import { FC } from 'react';
import { TooltipProps } from '../../../../../Tooltip/Tooltip';
export type VerifiedEmailIconProps = {
    className?: string;
    overwriteTooltipProps?: Partial<TooltipProps>;
};
export declare const VerifiedEmailIcon: FC<VerifiedEmailIconProps>;
